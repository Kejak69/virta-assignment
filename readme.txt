run_anroid_tests.sh script should start appium and android emulator in case paths are correctly defined in env variables. Also adb path needs to defined.
Rows which executes appium and emulator can be commented out if those are want to started  manually.

Emulator name used in run_android_tests.sh is 'Pixel_5_API_31'.

Atleast one contact needs to be created before hand to get add contact test to work